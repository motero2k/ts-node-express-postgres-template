name: CI

on:
    push:
        branches: ['main', 'develop']
    pull_request:
        branches: ['main', 'develop']

jobs:
    build-and-test:
        name: Build, Lint, Type-Check & Test
        runs-on: ubuntu-latest
        strategy:
            matrix:
                node-version: [24.x]
        steps:
            # Checkout code
            - name: Checkout repository
              uses: actions/checkout@v4

            # Setup Node.js and cache dependencies
            - name: Setup Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: 'npm'

            # Install dependencies (clean install for CI)
            - name: Install dependencies
              run: npm ci

            # Lint only src/ for TypeScript, markdown anywhere
            - name: Lint code
              run: npm run lint

            # Check code formatting
            - name: Check formatting
              run: npm run format:check

            # TypeScript strict type-check (no emit)
            - name: TypeScript type-check
              run: npx tsc --noEmit

            # Run tests with coverage
            - name: Run tests
              run: npm test
              env:
                  CI: true
    # THIS IS A TEMPLATE. UNCOMMENT AND CONFIGURE THE BELOW JOB TO ENABLE AUTOMATIC DOCKER IMAGE BUILDING AND PUSHING TO A REGISTRY ON PUSHES TO MAIN OR DEVELOP.
    # build-and-push:
    #     name: Build & Push Docker Images
    #     runs-on: ubuntu-latest
    #     needs: build-and-test
    #     if: >-
    #         github.event_name == 'push' &&
    #         (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')
    #     env:
    #         REGISTRY_IMAGE: motero2k/ts-node-express-postgres-template
    #         TAG: ${{ github.ref_name }}
    #     steps:
    #         - name: Checkout repository
    #           uses: actions/checkout@v4

    #         - name: Set up QEMU (multi-arch)
    #           uses: docker/setup-qemu-action@v3

    #         - name: Set up Docker Buildx
    #           uses: docker/setup-buildx-action@v3

    #         - name: Login to DockerHub
    #           uses: docker/login-action@v3
    #           with:
    #               username: ${{ secrets.DOCKERHUB_USERNAME }}
    #               password: ${{ secrets.DOCKERHUB_TOKEN }}

    #         - name: Build and Push DockerHub Image
    #           uses: docker/build-push-action@v6
    #           with:
    #               context: .
    #               file: Dockerfile
    #               platforms: linux/amd64,linux/arm64
    #               push: true
    #               cache-from: type=gha
    #               cache-to: type=gha,mode=max
    #               tags: |
    #                   ${{ env.REGISTRY_IMAGE }}:${{ env.TAG }}
    #                   ${{ github.ref == 'refs/heads/main' && format('{0}:latest', env.REGISTRY_IMAGE) || '' }}
    #                   ${{ github.ref == 'refs/heads/develop' && format('{0}:develop', env.REGISTRY_IMAGE) || '' }}
